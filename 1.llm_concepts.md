
1. 프롬프트 캐싱(prompt caching)
: LLM 호출 시 변하지 않는 내용을 메모리에 보관해두었다가 이후 호출에서 동일한 내용이 입력되면 보관된 정보를 재사용하는 최적화 기법

2. cache hit
: 캐시된 영역이 재사용되는 것

3. 자기회귀적 작동 방식 (autoregressive)
: 주어진 입력으로 다음 말을 생성한 뒤, 그 입력 + 방금 생성한 출력 전체를 다시 모델에 넣어 그다음 출력을 생성하는 과정
